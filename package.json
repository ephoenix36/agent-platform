{
  "name": "agents",
  "version": "0.1.0",
  "description": "A library of optimizable AI agent instructions with Evosuite integration",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "agents": "dist/cli.js",
    "agents-mcp": "dist/mcp/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "mcp-server": "node dist/mcp/server.js",
    "test": "vitest",
    "optimize": "python optimization/runner.py",
    "setup:storage": "pwsh ./scripts/setup-persistent-storage.ps1",
    "setup:storage-force": "pwsh ./scripts/setup-persistent-storage.ps1 -Force",
    "migrate:storage": "tsx scripts/migrate-to-persistent-storage.ts",
    "migrate:comprehensive": "npx tsx scripts/migrate-comprehensive.ts",
    "storage:stats": "tsx -e \"import { PersistentStorageManager } from './src/core/persistent-storage.js'; const s = new PersistentStorageManager(); await s.initialize(); console.log(JSON.stringify(await s.getStats(), null, 2));\"",
    "storage:backup": "tsx -e \"import { PersistentStorageManager } from './src/core/persistent-storage.js'; const s = new PersistentStorageManager(); await s.initialize(); const path = await s.createBackup(); console.log('Backup created:', path);\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "commander": "^12.0.0",
    "zod": "^3.22.4",
    "ajv": "^8.17.1",
    "dotenv": "^16.4.5",
    "chalk": "^5.3.0",
    "ora": "^8.0.1",
    "inquirer": "^9.2.12"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "tsx": "^4.7.0",
    "typescript": "^5.5.4",
    "vitest": "^3.2.4"
  },
  "keywords": [
    "ai",
    "agents",
    "optimization",
    "evosuite",
    "mcp",
    "prompt-engineering"
  ],
  "license": "MIT"
}
